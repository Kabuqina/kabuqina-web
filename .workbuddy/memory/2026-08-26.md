## 2026-08-26

- 统一了卡布奇娜主页与关于公司页（about.html）的导航栏：主页改用 `AboutNavbar`，并新增 `src/site-header.css` 作为两页共享的导航栏样式文件，锚点链接保留平滑滚动。
- 关于页首屏：移除整张海报图 about-love-logic-poster.png，新建 `src/components/HeroFloatingObjects.tsx`，把海报中的心形、光环、球体、立方体、窗口卡片、金字塔、尖括号、播放按钮、圆环、气泡拆成独立 SVG 元素，各自独立浮动动画（不同周期+负延迟）悬浮在首屏右侧玻璃卡片容器（`.about2-objects-wrap`）内。
- 排坑经验：无头 Edge 截图默认抓在入场动画（about2-rise 750ms fill both）播放途中，物件呈半透明、看起来"太淡"——实际样式没问题。截图需加 `--virtual-time-budget=6000` 让动画播完；再用 Python PIL 做像素饱和度分析（vivid 占比 ~10%）客观验证物件可见度。
- 根据反馈将扁平渐变物件重绘为 3D 玻璃/金属质感：爱心改为粉+奶油双色半透明玻璃并保留金色土星环，立方体、浏览器卡片、播放按钮使用透明/磨砂玻璃，圆环、尖括号为金属光泽。最终桌面端与移动端截图验证通过。
- **用户最终否决了拆分物件的方案（"丑"），要求用回原海报图**：已完整回退——About.tsx 恢复 `.about2-poster-wrap` + `<img src="/about-love-logic-poster.png">`，about.css 海报样式从 git HEAD 精确恢复（含 opacity 0.6 + mask 渐隐 + about2-poster-float 动画、各断点、reduced-motion），删除 HeroFloatingObjects.tsx。经验：SVG 手绘模拟原图 3D 玻璃质感难以达到原图水准，此类需求优先直接用原图或让用户提供素材；回退时用 `git show HEAD:file` 提取原始块做精确还原，避免误伤其他未提交改动（如导航栏共享样式的调整）。
- 用户新需求：首屏右侧空白区域加入淡彩玻璃浮动物件（主题=爱/逻辑/软件，色系=淡粉/淡黄/淡紫/淡蓝）。新建 `src/components/HeroGlassObjects.tsx`，包含粉玻璃爱心、紫玻璃 `</>`、黄玻璃灯泡、紫玻璃立方体、粉玻璃光标、黄玻璃齿轮、蓝玻璃浏览器卡片和四色气泡；各自独立上下浮动动画。`About.tsx` 在 `about2-hero__inner` 右列挂载组件，`about.css` 完成定位、入场动画、响应式（900px/640px）和 prefers-reduced-motion 支持。桌面端与移动端效果均验证通过。
- 用户反馈“颜色减淡、去掉物体底下阴影、去掉太花花绿绿的渲染颜色”后，调整 `HeroGlassObjects.tsx` 与 `about.css`：① 删除 `.about2-glass-wrap::before` 四色模糊光斑背景；② 删除所有物件 SVG 底部的接触投影椭圆及不再使用的模糊滤镜；③ 所有渐变填充降低不透明度、颜色向白/浅奶油/浅粉紫/浅粉蓝偏移，边缘高光保留；④ 卡片玻璃 rim 与外阴影改为仅保留内高光，气泡彩色也减淡。TypeScript 编译通过，桌面端与移动端截图验证通过。经验：减淡透明度后要注意形体可读性，必要时靠白边/高光/焦斑维持轮廓。
- 用户反馈“爱心放大、颜色加深一点点”后，`about.css` 中 `.about2-glass--heart` 桌面端宽度从 `min(66%, 290px)` 提到 `min(80%, 365px)`、移动端从 `min(58%, 235px)` 提到 `min(70%, 285px)`；`HeroGlassObjects.tsx` 中爱心主体渐变 `#fad2e4 0.44 / #f5b9d3 0.42 / #ec9ec5 0.62`、边缘反光、内阴影和焦散均加深一档。TypeScript 编译通过，桌面端与移动端截图验证通过。
