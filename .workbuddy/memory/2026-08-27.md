# 2026-08-27

- 为项目生成并替换了导航栏 Logo。
  - 基于用户参考图生成线稿镂空风格咖啡杯 Logo，杯身融入 "ai & logic" 文字，透明背景。
  - 替换 `public/company-logo.png`，同时影响 `AboutNavbar.tsx` 与 `AboutFooter.tsx` 中的 Logo 展示。
  - 开发服务器在端口 3001 运行，首页预览已刷新。
  - 放大导航栏 Logo：`.company-wordmark__logo` 桌面端 38px→48px，移动端(≤640px) 30px→38px。
  - 重新生成纯线稿/镂空版本 Logo 替换 `public/company-logo.png`，只保留黑色描边、字母 "ai & logic" 作为杯身镂空线条。
  - 再次放大：桌面端 48px→56px，移动端 38px→44px。
  - 改回原图（保留所有颜色细节）后只去米白背景：写了 `scripts/remove_bg.py`（Pillow + flood-fill），用 4 邻域从图片四角向外扩散，只把"连通到边缘 + 颜色与背景米色 (253,244,229) 通道差 ≤40"的像素变透明，杯内白色（被紫色描边包围）被完整保留。生成透明背景 PNG，替换 `public/company-logo.png`。
  - 桌面端 56px→64px，移动端 44px→50px。
  - 换用新参考图（手绘紫色描边咖啡杯，写 "logic"、爱心热气）。改 `remove_bg.py`：先裁掉底部 130px 去掉"豆包AI生成"水印，再 flood-fill（TH=45）去米黄背景。杯内/托盘内的米黄因被描边包围（不连通到边缘）被保留，外部背景变透明。
